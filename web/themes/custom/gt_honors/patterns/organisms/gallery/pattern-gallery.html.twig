{% set prefix = 'gallery' %}
{% set attributes = attributes.setAttribute('id', 'gallery--' ~ paragraph_id) %}
{% set classes = [
  prefix
] %}

{{ attach_library('gt_honors/parvus') }}

<div{{ attributes.addClass(classes) }}>
  <div class="container">
    {% if title %}
      <div class="{{ prefix }}_header">{{ title }}</div>
    {% endif %}
    <div class="{{ prefix }}__images row">
      {{ images }}
    </div>
  </div>
</div>
